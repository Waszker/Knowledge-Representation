using KR.Main.Entities;
using KR.Main.Entities.Conditions;
using KR.Main.Entities.Statements;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace KR.Test
{
    [TestClass]
    public class LastTests
    {
        [TestMethod]
        public void Load()
        {
            /*var domain = new Domain();
            var a = new Fluent("a");
            var l = new Fluent("l");
            domain.AddInitiallyClause(new Initially(new Conjunction(a, l)));
            var load = new Action("load");
            var Bill = new Actor("Bill");
            domain.AddCausesClause(new Causes(load, false, new List<Actor> { Bill }, l));
            var shoot = new Action("shoot");
            domain.AddCausesClause(new Causes(Ac))
                    shoot Ac causes ~l
            shoot Ac typically causes ~a if l*/
        }
    }
}
